# Component-local deps for the ISV web app (NOT added to the root requirements).
# The data layer relies on the already-installed scientific stack (numpy, pandas, scipy,
# anndata) used by isoform_structure_view.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
