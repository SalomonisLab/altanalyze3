fastapi>=0.110,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
jinja2>=3.1,<4
plotly>=6,<7
flask>=3,<4
anndata>=0.10,<0.13
scanpy>=1.10,<2
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.13,<2
matplotlib>=3.8,<4
tqdm>=4.66,<5
h5py>=3.10,<4
igraph>=0.11,<1.1
# ICGS.py imports nimfa at module scope, and cellHarmony_lite imports ICGS for its
# expression-scale check, so every alignment job needs it. Nothing else in this file
# pulls it in. Pinned to the version requirements.txt and the compiled container
# requirements already use.
nimfa==1.4.0
cairocffi>=1.7,<2
