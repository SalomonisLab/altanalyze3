"""
    Template function for mapping a feature to the associated transcript(s), then 
    converting the given coordinates - by getting the data from Ensemble
    Args:
        protein id (ENSP..)
    Returns:
        list: of converted coordinates, mapped to a `Feature` instance
"""